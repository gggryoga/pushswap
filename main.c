int main(int argc,char **argv)
{
	int i;
	t_stack *s;

	check_error(argc,argv);
	s = (t_list*)malloc(sizeof(t_list));
	if (a == NULL)
		exit (1);
	
	i = 0;
	while (i < argc)
	{
		ft_lstnew(argv[i]);
		ft_lstadd_back();
	}
}

sa;

ab;
ss;


